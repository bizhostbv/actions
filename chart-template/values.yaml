image:
  repository: harbor.k8s-hotel.nl/PROJECTNAME/APPNAME
  tag: "0.0.0"          # overridden per-env by gitops values
  pullPolicy: IfNotPresent
replicaCount: 1
service:
  port: 80
  targetPort: 8080
resources:
  requests: { cpu: 50m, memory: 64Mi }
  limits:   { cpu: 500m, memory: 256Mi }
